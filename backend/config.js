var config = {
    expressPort: 4200, /*port the Express server should run on*/
    mongoURL: 'mongodb://localhost:27017/sluber' /*Changeable based on where Mongo instance is located*/
};

module.exports = config;